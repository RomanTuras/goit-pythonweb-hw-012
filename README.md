### goit-pythonweb-hw-12 (Fast API, auth, login, email verification, docs)

`source $(poetry env info --path)/bin/activate`

`docker run --name postgres-db -p 5432:5432 -e POSTGRES_PASSWORD=fE512 -d postgres`
